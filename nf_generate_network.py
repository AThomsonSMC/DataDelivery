'''
Here, the network is randomly constructed.  Each variable controls the min and max number of nodes of each type, they
default to the values specified in config.txt but can be manually specified at the console.  The graph will be output
as a pair of .csv's in the format nodes.csv:"#node,demand(-)/offer(+)" - arcs.csv:"#arc,#tail,#head,capacity,cost"
'''